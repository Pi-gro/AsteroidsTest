{
  "name": "asteroids",
  "version": "1.0.0",
  "main": "asteroids.audio.worklet.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "http-server": "^14.1.1",
    "install": "^0.13.0",
    "npm": "^11.2.0",
    "serve": "^14.2.4"
  }
}

{
  "scripts": {
    "build": "echo 'No build step needed for Godot' && exit 0"
  }
}
